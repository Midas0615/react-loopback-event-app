import styled from 'styled-components'
import theme from 'styles/theme'

export const FormGroup = styled.div`
  margin: 1rem 0;
  width: 100%;
`
export const FormLabel = styled.div`
  color: ${theme.dark.lighten(0.3)};
  font-weight: 400;
  font-size: 0.9rem;
  padding-bottom: 3px;
`
